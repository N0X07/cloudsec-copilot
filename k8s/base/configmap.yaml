apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudsec-copilot-config
  namespace: cloudsec-copilot
  labels:
    app.kubernetes.io/name: cloudsec-copilot
data:
  APP_ENV: kubernetes-local
  DATABASE_URL: sqlite:////data/cloudsec.db
  OPENAI_MODEL: gpt-5.6-terra
  MAX_AGENT_STEPS: "4"

apiVersion: v1
kind: Secret
metadata:
  name: cloudsec-copilot-secrets
  namespace: cloudsec-copilot
  labels:
    app.kubernetes.io/name: cloudsec-copilot
type: Opaque
stringData:
  # Example only. Create real secrets with kubectl, a sealed secret, or an
  # external secret operator. Do not commit real API keys.
  OPENAI_API_KEY: ""
